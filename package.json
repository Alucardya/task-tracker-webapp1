{
  "name": "task-tracker-webapp",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "start": "python bot.py",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix task-tracker-client && npm run build --prefix task-tracker-client"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
